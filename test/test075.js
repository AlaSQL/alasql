if (typeof exports === 'object') {
	var assert = require('assert');
	var alasql = require('..');
}

test.todo('Test 75 - NULL, IS NULL');
describe.skip('Test 75 - NULL, IS NULL', function () {
	//	it.skip('localStorage', function(done){
	//		done();
	//	});
});
