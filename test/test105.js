if (typeof exports === 'object') {
	var assert = require('assert');
	var alasql = require('..');
}

test.todo('Test 105 - Synchronization over browsers and Node.js');
describe.skip('Test 105 - Synchronization over browsers and Node.js', function () {});
